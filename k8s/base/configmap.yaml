# ============================================
# Nexus LMS - ConfigMap
# Non-sensitive configuration values
# ============================================
apiVersion: v1
kind: ConfigMap
metadata:
  name: nexus-config
  labels:
    app: nexus-lms
data:
  NODE_ENV: "production"
  PORT: "3000"
  LOG_LEVEL: "info"
  CORS_ORIGIN: "*"
