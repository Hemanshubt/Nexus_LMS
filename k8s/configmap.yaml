# ================================
# ConfigMap - Application Configuration
# ================================
apiVersion: v1
kind: ConfigMap
metadata:
  name: nexus-lms-config
  namespace: nexus-lms
  labels:
    app.kubernetes.io/name: nexus-lms
data:
  NODE_ENV: "production"
  PORT: "3000"
  LOG_LEVEL: "info"
  CORS_ORIGIN: "https://nexus-lms.example.com"
